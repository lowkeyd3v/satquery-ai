# ===========================================================================
# SatQuery AI — Optional GPU & Local VLM Training Dependencies
# ===========================================================================
# Only required if SATQUERY_REAL_MODE=1 and running on a CUDA GPU machine.
# Not needed for default demo mode or cloud serverless deployment (Vercel).

# Geospatial stack
rasterio>=1.3.11
geojson>=3.1.0
shapely>=2.0.4
pyproj>=3.6.1
numpy>=1.26.4
pillow>=10.3.0

# Vision-Language / AI inference stack
torch>=2.4.0
torchvision>=0.19.0
transformers>=4.44.0
open-clip-torch>=2.24.0
huggingface-hub>=0.24.0
accelerate>=0.31.0
